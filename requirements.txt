Django==4.2.14
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
psycopg2-binary==2.9.9
django-cors-headers==4.4.0
python-decouple==3.8
celery==5.4.0
redis==5.0.8
django-celery-beat==2.6.0
Pillow==10.4.0
numpy==1.26.4
# face_recognition depends on the real "dlib" package, which has no prebuilt
# wheel on most platforms and can take 10+ minutes / 2GB+ RAM to compile from
# source — too much for a free-tier host. dlib-bin ships a prebuilt dlib, so
# face_recognition must be installed separately with --no-deps afterward (see
# render.yaml's buildCommand) so pip doesn't also try to build the real one.
dlib-bin==20.0.1
face_recognition_models==0.3.0
requests==2.32.3
supabase==2.7.4
pgvector==0.3.2
gunicorn==22.0.0
whitenoise==6.7.0
setuptools<81
