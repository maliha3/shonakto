from django.apps import AppConfig


class FoundPersonsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.found_persons"
    verbose_name = "Found / Unidentified Persons"
